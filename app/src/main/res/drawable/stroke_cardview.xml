<!-- res/drawable/cardview_background.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background Color -->
    <item>
        <shape android:shape="rectangle" >
            <solid android:color="@android:color/white"/> <!-- Ganti dengan warna background yang diinginkan -->
        </shape>
    </item>
    <!-- Border -->
    <item android:left="1dp" android:right="1dp" android:top="1dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent"/>
            <corners android:radius="10dp"/>
            <stroke
                android:width="3dp" android:color="@android:color/holo_green_dark"/> <!-- Ganti dengan warna hijau -->
        </shape>
    </item>
</layer-list>
